# ---------------------------------------------
# 📊 DATA PROCESSING & ANALYSIS
# ---------------------------------------------
pandas>=2.2.0          # Data manipulation and analysis — core for working with tabular data
numpy>=1.26.0          # Numerical operations, arrays, and matrix support (used by almost all ML libraries)

# ---------------------------------------------
# 🧠 MACHINE LEARNING
# ---------------------------------------------
scikit-learn>=1.9.0   # Classic ML models (regression, classification, clustering), preprocessing, model evaluation
xgboost>=3.3.0

# ---------------------------------------------
# 📈 VISUALIZATION
# ---------------------------------------------
matplotlib>=3.9.0      # Updated to support modern Python and NumPy 1.26+/2.0 without compilation errors
seaborn>=0.13.0        # Updated for compatibility with newer Pandas and Matplotlib structures

# ---------------------------------------------
# 📦 EXPERIMENT TRACKING & MODEL MANAGEMENT
# ---------------------------------------------
mlflow>=2.15.0         # Updated to prevent internal import breaks on modern Python runtimes

# ---------------------------------------------
# ✅ TESTING
# ---------------------------------------------
pytest>=8.0.0          # Updated for full compatibility with Python 3.14 internals

# ---------------------------------------------
# ⚡ API DEVELOPMENT (FOR MODEL SERVING)
# ---------------------------------------------
fastapi>=0.137.0       # Lightweight, high-performance web framework for serving ML models via REST APIs
uvicorn>=0.30.0        # ASGI server for running FastAPI apps — lightweight and fast

# ---------------------------------------------
# ⚙️ MISC
# ---------------------------------------------
pyyaml>=6.0.1
joblib>=1.4.0
setuptools>=70.0.0     # Fixed! Replaced old 65.5.0, which generated the 'pkg_resources' build error
ipykernel>=6.29.5
